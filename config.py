class config(object):
    top = 18    # diods count on the top
    right = 12  # diods count on the right
    bottom = 18 # diods count on the bottom
    left = 12   # diods count on the left
    
    height = 10 # diods count on the top
    width = 10
    
    offsetTop = 0               # offset from Top to grab right and left parts of screen
    offsetBottom = offsetTop    # offset from Bottom to grab right and left parts of screen
    
    offsetRight = 0             # offset from Right to grab top and bottom parts of screen
    offsetLeft = offsetRight    # offset from Left to grab top and bottom parts of screen
    
    step = 2    # uses for Retina display, whoose pixel density is 2x more